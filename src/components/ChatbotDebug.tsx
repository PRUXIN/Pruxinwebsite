// This component is no longer needed since we've replaced the external chatbot
// with a custom implementation. Keeping as placeholder in case debugging is needed.

export default function ChatbotDebug() {
  return null;
}